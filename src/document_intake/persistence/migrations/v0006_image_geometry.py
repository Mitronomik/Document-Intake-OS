# ruff: noqa: E501
"""PR-010 image geometry recipe tables and audit enum extension."""

from document_intake.persistence.migrations.model import Migration, migration_checksum

STATEMENTS: tuple[str, ...] = (
    "ALTER TABLE audit_events RENAME TO audit_events_v0005",
    """CREATE TABLE audit_events(event_id TEXT PRIMARY KEY NOT NULL CHECK(length(event_id)=36), occurred_at_utc TEXT NOT NULL CHECK(length(occurred_at_utc)>=20 AND substr(occurred_at_utc,-1)='Z'), actor_id TEXT NOT NULL CHECK(length(actor_id)=36), actor_kind TEXT NOT NULL CHECK(actor_kind IN ('OPERATOR','ADMIN','SYSTEM')), action_code TEXT NOT NULL CHECK(action_code IN ('ENTITY_CREATED','ENTITY_UPDATED','FIELD_CORRECTED','FIELD_VERIFIED','SNAPSHOT_CREATED','ARTIFACT_REGISTERED','EXPORT_CREATED','IMAGE_QUALITY_ASSESSED','IMAGE_GEOMETRY_RECIPE_CREATED')), subject_type TEXT NOT NULL CHECK(subject_type IN ('PERSON','IDENTITY_DOCUMENT','MIGRATION_DOCUMENT','VEHICLE','DOCUMENT','FIELD_CANDIDATE','APPLICATION','APPLICATION_SNAPSHOT','STORED_ARTIFACT','IMAGE_QUALITY_ASSESSMENT','IMAGE_GEOMETRY_RECIPE')), subject_id TEXT NOT NULL CHECK(length(subject_id)=36), field_key TEXT NULL CHECK(field_key IS NULL OR (length(field_key) BETWEEN 1 AND 128 AND substr(field_key,1,1) GLOB '[a-z0-9]' AND field_key NOT GLOB '*[^a-z0-9_.]*')), before_classification TEXT NULL CHECK(before_classification IS NULL OR before_classification IN ('ABSENT','NON_SENSITIVE','SENSITIVE_REDACTED')), before_was_present INTEGER NULL CHECK(before_was_present IS NULL OR before_was_present IN (0,1)), before_display_value TEXT NULL CHECK(before_display_value IS NULL OR (length(before_display_value) BETWEEN 1 AND 64 AND substr(before_display_value,1,1) GLOB '[A-Z0-9]' AND before_display_value NOT GLOB '*[^A-Z0-9_.:-]*')), after_classification TEXT NULL CHECK(after_classification IS NULL OR after_classification IN ('ABSENT','NON_SENSITIVE','SENSITIVE_REDACTED')), after_was_present INTEGER NULL CHECK(after_was_present IS NULL OR after_was_present IN (0,1)), after_display_value TEXT NULL CHECK(after_display_value IS NULL OR (length(after_display_value) BETWEEN 1 AND 64 AND substr(after_display_value,1,1) GLOB '[A-Z0-9]' AND after_display_value NOT GLOB '*[^A-Z0-9_.:-]*')), reason_code TEXT NULL CHECK(reason_code IS NULL OR (length(reason_code) BETWEEN 1 AND 64 AND substr(reason_code,1,1) GLOB '[A-Z]' AND reason_code NOT GLOB '*[^A-Z0-9_]*')), correlation_id TEXT NULL CHECK(correlation_id IS NULL OR length(correlation_id)=36), payload TEXT NOT NULL, CHECK((before_classification IS NULL AND before_was_present IS NULL AND before_display_value IS NULL) OR (before_classification='ABSENT' AND before_was_present=0 AND before_display_value IS NULL) OR (before_classification='NON_SENSITIVE' AND before_was_present=1 AND before_display_value IS NOT NULL) OR (before_classification='SENSITIVE_REDACTED' AND before_was_present=1 AND before_display_value IS NULL)), CHECK((after_classification IS NULL AND after_was_present IS NULL AND after_display_value IS NULL) OR (after_classification='ABSENT' AND after_was_present=0 AND after_display_value IS NULL) OR (after_classification='NON_SENSITIVE' AND after_was_present=1 AND after_display_value IS NOT NULL) OR (after_classification='SENSITIVE_REDACTED' AND after_was_present=1 AND after_display_value IS NULL)))""",
    "INSERT INTO audit_events SELECT * FROM audit_events_v0005",
    "DROP TABLE audit_events_v0005",
    "CREATE INDEX audit_events_subject_order_idx ON audit_events(subject_type, subject_id, occurred_at_utc, event_id)",
    "CREATE INDEX audit_events_correlation_order_idx ON audit_events(correlation_id, occurred_at_utc, event_id) WHERE correlation_id IS NOT NULL",
    "CREATE TRIGGER audit_events_no_update BEFORE UPDATE ON audit_events BEGIN SELECT RAISE(ABORT, 'audit_events immutable'); END",
    "CREATE TRIGGER audit_events_no_delete BEFORE DELETE ON audit_events BEGIN SELECT RAISE(ABORT, 'audit_events immutable'); END",
    "CREATE TRIGGER audit_events_no_replace BEFORE INSERT ON audit_events WHEN EXISTS (SELECT 1 FROM audit_events WHERE event_id = NEW.event_id) BEGIN SELECT RAISE(ABORT, 'audit_events duplicate'); END",
    """CREATE TABLE image_geometry_recipes(recipe_version_id TEXT PRIMARY KEY NOT NULL CHECK(length(recipe_version_id)=36), source_file_id TEXT NOT NULL REFERENCES source_files(id), superseded_recipe_version_id TEXT NULL REFERENCES image_geometry_recipes(recipe_version_id), revision INTEGER NOT NULL CHECK(revision >= 1), coordinate_space TEXT NOT NULL CHECK(coordinate_space='SOURCE_EFFECTIVE_PIXELS_V1'), source_effective_width INTEGER NOT NULL CHECK(source_effective_width >= 1), source_effective_height INTEGER NOT NULL CHECK(source_effective_height >= 1), quarter_turn INTEGER NOT NULL CHECK(quarter_turn IN (0,90,180,270)), top_left_x INTEGER NOT NULL CHECK(top_left_x >= 0 AND top_left_x <= source_effective_width), top_left_y INTEGER NOT NULL CHECK(top_left_y >= 0 AND top_left_y <= source_effective_height), top_right_x INTEGER NOT NULL CHECK(top_right_x >= 0 AND top_right_x <= source_effective_width), top_right_y INTEGER NOT NULL CHECK(top_right_y >= 0 AND top_right_y <= source_effective_height), bottom_right_x INTEGER NOT NULL CHECK(bottom_right_x >= 0 AND bottom_right_x <= source_effective_width), bottom_right_y INTEGER NOT NULL CHECK(bottom_right_y >= 0 AND bottom_right_y <= source_effective_height), bottom_left_x INTEGER NOT NULL CHECK(bottom_left_x >= 0 AND bottom_left_x <= source_effective_width), bottom_left_y INTEGER NOT NULL CHECK(bottom_left_y >= 0 AND bottom_left_y <= source_effective_height), pipeline_id TEXT NOT NULL CHECK(pipeline_id='PILLOW_QUAD_BICUBIC'), pipeline_version INTEGER NOT NULL CHECK(pipeline_version=1), created_at_utc TEXT NOT NULL CHECK(length(created_at_utc)>=20 AND substr(created_at_utc,-1)='Z'), canonical_payload TEXT NOT NULL CHECK(length(canonical_payload)>=1), UNIQUE(source_file_id, revision), UNIQUE(superseded_recipe_version_id), CHECK((revision=1 AND superseded_recipe_version_id IS NULL) OR (revision>1 AND superseded_recipe_version_id IS NOT NULL)))""",
    "CREATE INDEX image_geometry_recipes_source_order_idx ON image_geometry_recipes(source_file_id, revision, created_at_utc, recipe_version_id)",
    "CREATE TRIGGER image_geometry_recipes_no_update BEFORE UPDATE ON image_geometry_recipes BEGIN SELECT RAISE(ABORT, 'image_geometry_recipes_append_only'); END",
    "CREATE TRIGGER image_geometry_recipes_no_delete BEFORE DELETE ON image_geometry_recipes BEGIN SELECT RAISE(ABORT, 'image_geometry_recipes_append_only'); END",
    "CREATE TRIGGER image_geometry_recipes_no_replace BEFORE INSERT ON image_geometry_recipes WHEN EXISTS (SELECT 1 FROM image_geometry_recipes WHERE recipe_version_id = NEW.recipe_version_id) BEGIN SELECT RAISE(ABORT, 'image_geometry_recipes_duplicate'); END",
    "PRAGMA user_version = 6",
)
MIGRATION = Migration(6, "image_geometry_pr010", STATEMENTS, migration_checksum(STATEMENTS))
